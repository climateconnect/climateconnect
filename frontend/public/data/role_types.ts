//Important: These are ordered by the strenght of the role from weak to strong
const ROLE_TYPES = {
  read_only_type: "read only",
  read_write_type: "read write",
  all_type: "all",
};

export default ROLE_TYPES;
